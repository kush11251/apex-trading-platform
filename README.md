# Apex Trading Platform
A high-performance trading platform with automated strategy execution.
## Installation
1. Clone the repository and navigate to the project directory.
2. Run `go mod init apex-trading-platform` and `go mod tidy` to initialize and install dependencies.
## Execution
Run `go run src/main.go` to start the platform.
